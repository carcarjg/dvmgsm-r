<UserControl x:Class="dvmconsole.Controls.SystemStatusBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Width="145" Height="45">
    <UserControl.Effect>
        <DropShadowEffect ShadowDepth="1" BlurRadius="16" Color="#FF686767" Opacity="0.3" />
    </UserControl.Effect>
    <Border x:Name="ControlBorder" BorderBrush="LightGray" BorderThickness="1,1,1,1" CornerRadius="6">
        <StackPanel Margin="2,2,2,2">
            <TextBlock Text="{Binding SystemName}" Margin="0,1,0,0" FontWeight="Bold" Foreground="White" FontSize="16" TextAlignment="Center" />
            <TextBlock Text="{Binding ConnectionState}" Margin="0,4,0,0" Foreground="Gold" FontSize="10" TextAlignment="Center" />
        </StackPanel>
    </Border>
</UserControl>
